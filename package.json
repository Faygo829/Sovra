{
  "name": "guardian_executor",
  "version": "0.1.0",
  "description": "Stellar/Soroban contract for AI decision enforcement",
  "main": "dist/index.js",
  "scripts": {
    "test": "cargo test -p guardian_executor_soroban",
    "build": "cargo build -p guardian_executor_soroban",
    "deploy": "echo 'Deploy with: soroban contract deploy --network testnet --source <identity> --wasm target/wasm32-unknown-unknown/release/guardian_executor_soroban.wasm'"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@noble/hashes": "^2.2.0",
    "@solana/web3.js": "^1.91.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.0",
    "@types/node": "^20.0.0",
    "mocha": "^10.2.0",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
