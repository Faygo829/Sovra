{
  "name": "sovra-stellar",
  "private": true,
  "scripts": {
    "test": "cargo test --manifest-path ./contracts/hello_world/Cargo.toml",
    "build": "cargo build --manifest-path ./contracts/hello_world/Cargo.toml"
  }
}
