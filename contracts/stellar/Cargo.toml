[package]
name = "guardian_executor_soroban"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
soroban-sdk = { version = "25.1.0" }
wee_alloc = "0.4"

[dev-dependencies]
soroban-sdk = { version = "25.1.0", features = ["testutils"] }
